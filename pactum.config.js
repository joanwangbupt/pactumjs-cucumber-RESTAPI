module.exports = {
    host: 'https://example.com', // API base URL
    spec: './features/**/*.feature', // Location of feature files
    tags: '@api', // Specify Cucumber tags to filter which scenarios to run
  };
  